#include <vector>
#include <iostream>

template<typename T>
void bubble_sort(std::vector<T>) {
    bool sorted = true;
    while () {
        T prev = null;
        for (T item : v) {

        }
    }
}

int main() {

    return 0;
}